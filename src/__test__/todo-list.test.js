/**
 * @jest-environment jsdom
 */

const addTodo =  require('../index');

describe("Adding an item", ()=> {
  it('checked argument to addTodo item matches description in localstorage', ()=> {

  });
});